#include "main.h"
/**
 * _isupper - function that checks for uppercase character
 *@c: the character to be checked
 *Return: 1 Success
 */
int _isupper(int c)
{
if (isupper(c))
{
	return (1);
}
else
{
	return (0);
}
}
