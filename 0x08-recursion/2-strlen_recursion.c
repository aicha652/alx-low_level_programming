#include "main.h"
/**
 * _strlen_recursion - function that returne the length of a string
 * @s: string
 * Return: length of a string
 */
int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
